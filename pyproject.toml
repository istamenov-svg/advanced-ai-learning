[project]
name = "aihc"
version = "0.1.0"
description = "Advanced AI learning project: grounded, repeatable analytics on healthcare marketing data"
requires-python = ">=3.11"

[tool.setuptools.packages.find]
where = ["src"]

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]
